# project-task-master-day-4
Create a generic Resource model (fields like title, description, category, status, amount, createdBy) Create CRUD routes: Add Get all Get single Update Delete Link each resource to the logged-in user
